import { GenreCompactPipe } from './genre-compact.pipe';

describe('GenreCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
