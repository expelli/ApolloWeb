import { CategoryCompactPipe } from './category-compact.pipe';

describe('CategoryCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
