import { VideoUrlIframePipe } from './video-url-iframe.pipe';

describe('VideoUrlIframePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoUrlIframePipe();
    expect(pipe).toBeTruthy();
  });
});
