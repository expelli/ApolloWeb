import { ScreeningCompactPipe } from './screening-compact.pipe';

describe('ScreeningCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreeningCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
