import { ApiDatePipePipe } from './api-date-pipe.pipe';

describe('ApiDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
