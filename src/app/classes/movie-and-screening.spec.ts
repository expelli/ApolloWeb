import { MovieAndScreening } from './movie-and-screening';

describe('MovieAndScreening', () => {
  it('should create an instance', () => {
    expect(new MovieAndScreening()).toBeTruthy();
  });
});
