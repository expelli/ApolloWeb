import { Base64MovieImageOption } from './base64-movie-image-option';

describe('Base64MovieImageOption', () => {
  it('should create an instance', () => {
    expect(new Base64MovieImageOption()).toBeTruthy();
  });
});
