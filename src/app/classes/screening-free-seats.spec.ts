import { ScreeningFreeSeats } from './screening-free-seats';

describe('ScreeningFreeSeats', () => {
  it('should create an instance', () => {
    expect(new ScreeningFreeSeats()).toBeTruthy();
  });
});
